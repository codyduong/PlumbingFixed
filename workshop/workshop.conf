# Steam Workshop page metadata (source of truth for scripts/publish-workshop.*).
# Flat key=value, single-line values only, '#' comments. The description lives
# verbatim in workshop/description.bbcode. See docs/RELEASING.md.
title=PlumbingFixed [B42.19+]
tags=Build 42;QoL;Interface
visibility=public
# Publish targets — `mise run publish <test|prod>` selects between these (test first!).
published_id=3626008449
test_published_id=3680940911
app_id=108600
preview=preview.png
