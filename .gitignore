# Packaging staging area (scripts/package.*)
/dist/
# Ephemeral dedicated-server world/state (scripts/testhost.ps1; seed configs in testhost/)
/.testhost/

# Decompiled game source + vanilla Lua snapshots (analysis only, produced by
# scripts/decompile.ps1); one subfolder per PZ build (Major.Minor.Patch, since patches can
# change Java behavior too), e.g. .decompiled/42.19.0/, .decompiled/42.20.2/, kept side by
# side during an update (docs/UPDATING-PZ.md)
/.decompiled*/
# Downloaded dev tools (e.g. Zomboid Decompiler)
/.tools/
# Steam Workshop publish scratch (generated VDF, steamcmd logs)
/.publish/
# mise local, machine-specific overrides
mise.local.toml